# backend-service

Simple Express + TypeScript backend used in the project.

Run locally (PowerShell):

```powershell
cd services/backend-service
npm install
npm run dev
```

Build & run (production):

```powershell
cd services/backend-service
npm run build
npm start
```
